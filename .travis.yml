dist: xenial
addons:
  apt:
    sources:
    - git-core
    packages:
    - git
  snaps:
  - name: hugo
    channel: 0.59.0/stable

language: python
python:
- '3.7'

branches:
  only: 
    - master

script: make gh-pages-on-travisci
